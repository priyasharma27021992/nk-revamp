import EmbedSocialWidget from '@/components/EmbedSocialWidget';

const Gallery = () => {
	return (
		<main>
			<section>
				<EmbedSocialWidget />
			</section>
		</main>
	);
};

export default Gallery;
